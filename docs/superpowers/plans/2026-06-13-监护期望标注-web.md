# 监护期望标注（步骤核查点）— Web 端实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 SmartSOP（Vue3 + FastAPI + MySQL）里，给步骤节点新增「监护期望（核查点）」的全栈编写能力——一个步骤可挂 0..N 个核查点，第一期支持 `ocr`（读数/文字核对）与 `safety`（安全/合规）两类。

**Architecture:** 复用现有统一节点模型 `ProcedureNode`（`kind='step'`）。新增子表 `tb_procedure_node_check`，每行一个核查点，类型专属字段存 JSON `params`。后端走既有分层（model → schema → service → router，service flush / router commit），多租户靠现有 `before_flush` 事件自动注入 `company_id`。前端在步骤详情面板 `NodeDetailPanel.vue` 旁挂一个「监护」子面板。对应设计 spec D2/D3、§6 编写侧。

**Tech Stack:** SQLAlchemy 2.0（Mapped/mixins，`with_variant` 兼容 MySQL/SQLite）、Alembic、Pydantic v2、FastAPI、pytest（内存 SQLite + `db`/`client` 夹具）、Vue 3 + Pinia + TypeScript + Vite。

---

## 范围与边界

- **做**：核查点的数据模型、迁移、schema、service、REST API、编写端「监护」面板（增删查 + 类型动态表单）。
- **第一期只启用** `check_type ∈ {ocr, safety}`；`{object, action, semantic}` 在校验里**拒绝**（框架预留，返回 422）。
- **不做**（后续计划）：SOP 参考关系、图纸/附件、监护运行时消费这些核查点（属后端执行服务 / 端侧）。

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `backend/app/models/node_check.py` | `ProcedureNodeCheck` ORM 模型 | 新建 |
| `backend/app/models/__init__.py` | 注册新模型到 `Base.metadata` | 修改 |
| `backend/alembic/versions/0003_node_check.py` | 建表迁移（生产 MySQL） | 新建 |
| `backend/app/schemas/node_check.py` | `CheckOut/CheckCreateIn/CheckPatchIn` | 新建 |
| `backend/app/services/node_check_service.py` | 增删查改 + 校验 + 守卫 | 新建 |
| `backend/app/routers/node_checks.py` | REST 端点 | 新建 |
| `backend/app/main.py` | 注册 router | 修改 |
| `backend/tests/test_node_check_model.py` | 模型测试 | 新建 |
| `backend/tests/test_node_check_service.py` | service 测试 | 新建 |
| `backend/tests/test_node_checks_api.py` | API 测试 | 新建 |
| `frontend/src/api/checks.ts` | 前端 API 客户端 | 新建 |
| `frontend/src/components/editor/NodeCheckPanel.vue` | 监护面板组件 | 新建 |
| `frontend/src/components/editor/NodeDetailPanel.vue` | 集成监护面板（kind==='step' 时显示） | 修改 |

## 约定速记（实现者必读）

- 模型继承 `Base, UUIDMixin, TimestampMixin, SoftDeleteMixin, TenantMixin`（见 `app/models/base.py`）。**不要手动设 `company_id`**——`before_flush` 事件按当前租户上下文自动注入。
- 业务表软删：写入 `is_active=False, deleted_at=utcnow()`，查询过滤 `is_active=True`。
- service 抛 `HTTPException(status_code=..., detail={"code": "...", "message": "..."})`，与 `node_service` 一致。
- 编辑前置守卫：`node_service._assert_procedure_editable(db, procedure_id)`（PUBLISHED 程序拒写，返回 400 `PROCEDURE_READONLY`）。
- 测试建租户行需 `pytestmark = pytest.mark.usefixtures("_tenant_ctx")`，否则 `before_flush` fail-closed 抛 `TenantContextMissingError`。
- 端口/启动见 `running-smartsop-dev` 技能（后端 8000 / 前端 5173；测试 `cd backend && pytest`）。

---

## Task 1: 数据模型 `ProcedureNodeCheck`

**Files:**
- Create: `backend/app/models/node_check.py`
- Modify: `backend/app/models/__init__.py`
- Test: `backend/tests/test_node_check_model.py`

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/test_node_check_model.py
"""ProcedureNodeCheck 模型：核查点挂到 step 节点。"""
from __future__ import annotations

import uuid

import pytest

from app.models.node import ProcedureNode
from app.models.node_check import ProcedureNodeCheck
from app.models.procedure import Procedure

pytestmark = pytest.mark.usefixtures("_tenant_ctx")


def _step_node(db) -> ProcedureNode:
    proc = Procedure(
        procedure_group_id=str(uuid.uuid4()),
        folder_id=str(uuid.uuid4()),
        code="QC-00001",
        name="P",
        level_of_use="reference",
        version=1,
        status="DRAFT",
        is_current=True,
    )
    db.add(proc)
    db.flush()
    node = ProcedureNode(
        procedure_id=proc.id, body="读压力表", kind="step", sort_order=1000
    )
    db.add(node)
    db.flush()
    return node


def test_create_and_query_check(db):
    node = _step_node(db)
    check = ProcedureNodeCheck(
        node_id=node.id,
        procedure_id=node.procedure_id,
        check_type="ocr",
        modality="visual",
        severity="warn",
        trigger="manual",
        prompt="请将压力表对准镜头",
        params={"target_desc": "压力表读数", "match_mode": "range", "expected": {"min": 0, "max": 0.5}, "unit": "MPa"},
        sort_order=1000,
    )
    db.add(check)
    db.commit()

    rows = db.query(ProcedureNodeCheck).filter_by(node_id=node.id, is_active=True).all()
    assert len(rows) == 1
    assert rows[0].check_type == "ocr"
    assert rows[0].params["unit"] == "MPa"
    assert rows[0].company_id  # before_flush 自动注入
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && pytest tests/test_node_check_model.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'app.models.node_check'`

- [ ] **Step 3: Write the model**

```python
# backend/app/models/node_check.py
"""步骤监护期望（核查点）模型 tb_procedure_node_check。

一个步骤节点(kind='step')可挂 0..N 个核查点。类型专属字段存 JSON params。
对应设计 spec §6 编写侧、D2/D3。第一期 check_type ∈ {ocr, safety}。
"""
from __future__ import annotations

from typing import TYPE_CHECKING, Any

from sqlalchemy import Float, ForeignKey, Index, Integer, String, JSON
from sqlalchemy.orm import Mapped, mapped_column

from app.models.base import (
    Base,
    SoftDeleteMixin,
    TenantMixin,
    TimestampMixin,
    UUIDMixin,
)

if TYPE_CHECKING:
    pass


class ProcedureNodeCheck(Base, UUIDMixin, TimestampMixin, SoftDeleteMixin, TenantMixin):
    """步骤核查点（监护期望）。"""

    __tablename__ = "tb_procedure_node_check"

    node_id: Mapped[str] = mapped_column(
        String(36), ForeignKey("tb_procedure_node.id", ondelete="CASCADE"), index=True
    )
    # 冗余存储便于按程序列出所有核查点（上下文/预览）。
    procedure_id: Mapped[str] = mapped_column(
        String(36), ForeignKey("tb_procedure.id", ondelete="RESTRICT"), index=True
    )
    check_type: Mapped[str] = mapped_column(String(20))  # ocr|safety|(预留 object|action|semantic)
    modality: Mapped[str] = mapped_column(String(20), default="visual", server_default="visual")
    severity: Mapped[str] = mapped_column(String(20), default="warn", server_default="warn")
    trigger: Mapped[str] = mapped_column(String(20), default="on_enter", server_default="on_enter")
    prompt: Mapped[str] = mapped_column(String(500), default="", server_default="")
    keep_evidence: Mapped[bool] = mapped_column(default=True, server_default="1")
    confidence_threshold: Mapped[float | None] = mapped_column(Float, nullable=True, default=None)
    params: Mapped[dict[str, Any]] = mapped_column(JSON, default=dict)
    sort_order: Mapped[int] = mapped_column(Integer, default=0, server_default="0")

    __table_args__ = (
        Index("ix_tb_procedure_node_check_node_id_sort_order", "node_id", "sort_order"),
    )
```

- [ ] **Step 4: Register the model**

In `backend/app/models/__init__.py`, add the import (alphabetically near `node`) and add to `__all__`:

```python
from app.models.node_check import ProcedureNodeCheck
```
Add `"ProcedureNodeCheck",` to the `__all__` list.

- [ ] **Step 5: Run test to verify it passes**

Run: `cd backend && pytest tests/test_node_check_model.py -v`
Expected: PASS (2 nodes created, 1 check row, `company_id` populated)

- [ ] **Step 6: Commit**

```bash
git add backend/app/models/node_check.py backend/app/models/__init__.py backend/tests/test_node_check_model.py
git commit -m "feat(checks): add ProcedureNodeCheck model for step supervision expectations"
```

---

## Task 2: Alembic 迁移（生产 MySQL 建表）

**Files:**
- Create: `backend/alembic/versions/0003_node_check.py`

> 说明：测试用 `Base.metadata.create_all` 自动建表，无需迁移；本迁移仅供生产 MySQL。

- [ ] **Step 1: 取当前迁移 head**

Run: `cd backend && alembic heads`
记下输出的 revision id（当前应为 `0002_drop_formtype_alert_types`）。下一步 `down_revision` 用它。

- [ ] **Step 2: 写迁移**

```python
# backend/alembic/versions/0003_node_check.py
"""create tb_procedure_node_check

Revision ID: 0003_node_check
Revises: 0002_drop_formtype_alert_types
"""
from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "0003_node_check"
down_revision = "0002_drop_formtype_alert_types"  # ← 若 Step 1 输出不同，改为实际 head
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        "tb_procedure_node_check",
        sa.Column("id", sa.String(36), primary_key=True),
        sa.Column("company_id", sa.String(36), sa.ForeignKey("tb_company.id", ondelete="CASCADE"), nullable=False, index=True),
        sa.Column("node_id", sa.String(36), sa.ForeignKey("tb_procedure_node.id", ondelete="CASCADE"), nullable=False, index=True),
        sa.Column("procedure_id", sa.String(36), sa.ForeignKey("tb_procedure.id", ondelete="RESTRICT"), nullable=False, index=True),
        sa.Column("check_type", sa.String(20), nullable=False),
        sa.Column("modality", sa.String(20), nullable=False, server_default="visual"),
        sa.Column("severity", sa.String(20), nullable=False, server_default="warn"),
        sa.Column("trigger", sa.String(20), nullable=False, server_default="on_enter"),
        sa.Column("prompt", sa.String(500), nullable=False, server_default=""),
        sa.Column("keep_evidence", sa.Boolean(), nullable=False, server_default="1"),
        sa.Column("confidence_threshold", sa.Float(), nullable=True),
        sa.Column("params", sa.JSON(), nullable=False),
        sa.Column("sort_order", sa.Integer(), nullable=False, server_default="0"),
        sa.Column("is_active", sa.Boolean(), nullable=False, server_default="1"),
        sa.Column("deleted_at", sa.DateTime(), nullable=True),
        sa.Column("created_at", sa.DateTime(), nullable=False),
    )
    op.create_index(
        "ix_tb_procedure_node_check_node_id_sort_order",
        "tb_procedure_node_check",
        ["node_id", "sort_order"],
    )


def downgrade() -> None:
    op.drop_index("ix_tb_procedure_node_check_node_id_sort_order", table_name="tb_procedure_node_check")
    op.drop_table("tb_procedure_node_check")
```

- [ ] **Step 3: 验证迁移可前后滚**

Run: `cd backend && DATABASE_URL=sqlite:///./_mig_check.db alembic upgrade head && DATABASE_URL=sqlite:///./_mig_check.db alembic downgrade -1 && rm -f _mig_check.db`
Expected: 无报错，upgrade 与 downgrade 各成功一次。

- [ ] **Step 4: Commit**

```bash
git add backend/alembic/versions/0003_node_check.py
git commit -m "feat(checks): alembic migration for tb_procedure_node_check"
```

---

## Task 3: Pydantic schemas

**Files:**
- Create: `backend/app/schemas/node_check.py`
- Test: `backend/tests/test_node_check_service.py`（本任务先放 schema 用例；service 用例在 Task 4 追加）

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/test_node_check_service.py
"""核查点 schema + service。"""
from __future__ import annotations

import uuid

import pytest
from pydantic import ValidationError

from app.schemas.node_check import CheckCreateIn

pytestmark = pytest.mark.usefixtures("_tenant_ctx")


def test_schema_accepts_ocr():
    m = CheckCreateIn(
        check_type="ocr",
        prompt="读数",
        params={"target_desc": "压力", "match_mode": "range", "expected": {"min": 0, "max": 0.5}},
    )
    assert m.check_type == "ocr"
    assert m.modality == "visual"  # 默认


def test_schema_rejects_unknown_type():
    with pytest.raises(ValidationError):
        CheckCreateIn(check_type="telepathy", params={})


def test_schema_rejects_bad_severity():
    with pytest.raises(ValidationError):
        CheckCreateIn(check_type="safety", severity="nuclear", params={"items": ["gloves"]})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && pytest tests/test_node_check_service.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'app.schemas.node_check'`

- [ ] **Step 3: Write the schemas**

```python
# backend/app/schemas/node_check.py
"""核查点 API schema（设计 spec §6）。"""
from __future__ import annotations

from typing import Any, Literal

from pydantic import BaseModel, ConfigDict, Field

CheckType = Literal["ocr", "safety"]  # 第一期启用；object/action/semantic 后续
Modality = Literal["visual", "voice", "dual"]
Severity = Literal["info", "warn", "critical"]
Trigger = Literal["on_enter", "manual", "continuous"]


class CheckOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: str
    node_id: str
    procedure_id: str
    check_type: str
    modality: str
    severity: str
    trigger: str
    prompt: str
    keep_evidence: bool
    confidence_threshold: float | None
    params: dict[str, Any]
    sort_order: int


class CheckCreateIn(BaseModel):
    model_config = ConfigDict(extra="forbid")

    check_type: CheckType
    modality: Modality = "visual"
    severity: Severity = "warn"
    trigger: Trigger = "on_enter"
    prompt: str = ""
    keep_evidence: bool = True
    confidence_threshold: float | None = None
    params: dict[str, Any] = Field(default_factory=dict)
    sort_order: int | None = None


class CheckPatchIn(BaseModel):
    model_config = ConfigDict(extra="forbid")

    modality: Modality | None = None
    severity: Severity | None = None
    trigger: Trigger | None = None
    prompt: str | None = None
    keep_evidence: bool | None = None
    confidence_threshold: float | None = None
    params: dict[str, Any] | None = None
    sort_order: int | None = None
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && pytest tests/test_node_check_service.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add backend/app/schemas/node_check.py backend/tests/test_node_check_service.py
git commit -m "feat(checks): pydantic schemas for node check points"
```

---

## Task 4: Service 层（增删查改 + 校验 + 守卫）

**Files:**
- Create: `backend/app/services/node_check_service.py`
- Test: `backend/tests/test_node_check_service.py`（追加 service 用例）

- [ ] **Step 1: Append failing service tests**

在 `backend/tests/test_node_check_service.py` 末尾追加：

```python
from app.models.node import ProcedureNode
from app.models.procedure import Procedure
from app.services import node_check_service
from fastapi import HTTPException


def _step(db, status: str = "DRAFT") -> ProcedureNode:
    proc = Procedure(
        procedure_group_id=str(uuid.uuid4()), folder_id=str(uuid.uuid4()),
        code="QC-00001", name="P", level_of_use="reference", version=1,
        status=status, is_current=True,
    )
    db.add(proc); db.flush()
    node = ProcedureNode(procedure_id=proc.id, body="读压力表", kind="step", sort_order=1000)
    db.add(node); db.flush()
    return node


def test_create_and_list(db):
    node = _step(db)
    c = node_check_service.create_check(db, node.id, {
        "check_type": "safety", "params": {"items": ["gloves"]}, "prompt": "戴手套",
    })
    db.commit()
    rows = node_check_service.list_checks(db, node.id)
    assert [r.id for r in rows] == [c.id]
    assert rows[0].procedure_id == node.procedure_id


def test_create_rejects_non_step_node(db):
    node = _step(db)
    node.kind = "node"; db.flush()
    with pytest.raises(HTTPException) as ei:
        node_check_service.create_check(db, node.id, {"check_type": "safety", "params": {"items": ["gloves"]}})
    assert ei.value.status_code == 400
    assert ei.value.detail["code"] == "NODE_NOT_STEP"


def test_create_rejects_ocr_without_target(db):
    node = _step(db)
    with pytest.raises(HTTPException) as ei:
        node_check_service.create_check(db, node.id, {"check_type": "ocr", "params": {}})
    assert ei.value.status_code == 422
    assert ei.value.detail["code"] == "CHECK_INVALID"


def test_create_rejects_on_published(db):
    node = _step(db, status="PUBLISHED")
    with pytest.raises(HTTPException) as ei:
        node_check_service.create_check(db, node.id, {"check_type": "safety", "params": {"items": ["gloves"]}})
    assert ei.value.status_code == 400
    assert ei.value.detail["code"] == "PROCEDURE_READONLY"


def test_delete_soft(db):
    node = _step(db)
    c = node_check_service.create_check(db, node.id, {"check_type": "safety", "params": {"items": ["gloves"]}})
    db.commit()
    node_check_service.delete_check(db, c.id); db.commit()
    assert node_check_service.list_checks(db, node.id) == []
```

- [ ] **Step 2: Run to verify failure**

Run: `cd backend && pytest tests/test_node_check_service.py -v`
Expected: FAIL with `ImportError: cannot import name 'node_check_service'`

- [ ] **Step 3: Write the service**

```python
# backend/app/services/node_check_service.py
"""核查点 service：增删查改 + 校验 + 编辑守卫。"""
from __future__ import annotations

from typing import Any

from fastapi import HTTPException
from sqlalchemy import select
from sqlalchemy.orm import Session

from app.models.base import utcnow
from app.models.node import ProcedureNode
from app.models.node_check import ProcedureNodeCheck
from app.services import node_service

_SORT_GAP = 1000


def _invalid(message: str) -> HTTPException:
    return HTTPException(status_code=422, detail={"code": "CHECK_INVALID", "message": message})


def _validate_params(check_type: str, params: dict[str, Any]) -> None:
    if check_type == "ocr":
        if not str(params.get("target_desc", "")).strip():
            raise _invalid("ocr 核查需要非空 target_desc")
        if params.get("match_mode") not in {"exact", "contains", "regex", "range"}:
            raise _invalid("ocr 核查 match_mode 必须是 exact/contains/regex/range")
    elif check_type == "safety":
        items = params.get("items")
        if not isinstance(items, list) or not items:
            raise _invalid("safety 核查需要非空 items 列表")
    else:  # pragma: no cover - schema 已挡，双保险
        raise _invalid(f"第一期不支持 check_type={check_type}")


def _get_step_node(db: Session, node_id: str) -> ProcedureNode:
    node = db.get(ProcedureNode, node_id)
    if node is None or not node.is_active:
        raise HTTPException(status_code=404, detail={"code": "NODE_NOT_FOUND", "message": node_id})
    if node.kind != "step":
        raise HTTPException(status_code=400, detail={"code": "NODE_NOT_STEP", "message": "仅 step 节点可挂核查点"})
    return node


def _get_check(db: Session, check_id: str) -> ProcedureNodeCheck:
    c = db.get(ProcedureNodeCheck, check_id)
    if c is None or not c.is_active:
        raise HTTPException(status_code=404, detail={"code": "CHECK_NOT_FOUND", "message": check_id})
    return c


def list_checks(db: Session, node_id: str) -> list[ProcedureNodeCheck]:
    stmt = (
        select(ProcedureNodeCheck)
        .where(ProcedureNodeCheck.node_id == node_id, ProcedureNodeCheck.is_active.is_(True))
        .order_by(ProcedureNodeCheck.sort_order)
    )
    return list(db.execute(stmt).scalars())


def create_check(db: Session, node_id: str, data: dict[str, Any]) -> ProcedureNodeCheck:
    node = _get_step_node(db, node_id)
    node_service._assert_procedure_editable(db, node.procedure_id)
    check_type = data["check_type"]
    params = data.get("params", {})
    _validate_params(check_type, params)

    if data.get("sort_order") is not None:
        sort_order = data["sort_order"]
    else:
        existing = list_checks(db, node_id)
        sort_order = (existing[-1].sort_order + _SORT_GAP) if existing else _SORT_GAP

    check = ProcedureNodeCheck(
        node_id=node.id,
        procedure_id=node.procedure_id,
        check_type=check_type,
        modality=data.get("modality", "visual"),
        severity=data.get("severity", "warn"),
        trigger=data.get("trigger", "on_enter"),
        prompt=data.get("prompt", ""),
        keep_evidence=data.get("keep_evidence", True),
        confidence_threshold=data.get("confidence_threshold"),
        params=params,
        sort_order=sort_order,
    )
    db.add(check)
    db.flush()
    return check


def patch_check(db: Session, check_id: str, changes: dict[str, Any]) -> ProcedureNodeCheck:
    check = _get_check(db, check_id)
    node_service._assert_procedure_editable(db, check.procedure_id)
    if "params" in changes and changes["params"] is not None:
        _validate_params(check.check_type, changes["params"])
    for key in ("modality", "severity", "trigger", "prompt", "keep_evidence", "confidence_threshold", "params", "sort_order"):
        if key in changes and changes[key] is not None:
            setattr(check, key, changes[key])
    db.flush()
    return check


def delete_check(db: Session, check_id: str) -> None:
    check = _get_check(db, check_id)
    node_service._assert_procedure_editable(db, check.procedure_id)
    check.is_active = False
    check.deleted_at = utcnow()
    db.flush()
```

- [ ] **Step 4: Run to verify pass**

Run: `cd backend && pytest tests/test_node_check_service.py -v`
Expected: PASS（schema 3 + service 5 = 8 passed）

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/node_check_service.py backend/tests/test_node_check_service.py
git commit -m "feat(checks): node_check_service with validation and editable guard"
```

---

## Task 5: REST Router + 注册

**Files:**
- Create: `backend/app/routers/node_checks.py`
- Modify: `backend/app/main.py`
- Test: `backend/tests/test_node_checks_api.py`

- [ ] **Step 1: Write the failing API test**

```python
# backend/tests/test_node_checks_api.py
"""核查点 REST API。复用 client 夹具（已带认证/租户重定向）。"""
from __future__ import annotations

import uuid

import pytest

from app.models.node import ProcedureNode
from app.models.procedure import Procedure

pytestmark = pytest.mark.usefixtures("_tenant_ctx")


@pytest.fixture
def step_node(db) -> ProcedureNode:
    proc = Procedure(
        procedure_group_id=str(uuid.uuid4()), folder_id=str(uuid.uuid4()),
        code="QC-00001", name="P", level_of_use="reference", version=1,
        status="DRAFT", is_current=True,
    )
    db.add(proc); db.flush()
    node = ProcedureNode(procedure_id=proc.id, body="读压力表", kind="step", sort_order=1000)
    db.add(node); db.commit()
    return node


def test_crud_flow(client, step_node):
    nid = step_node.id
    # create
    r = client.post(f"/api/v1/nodes/{nid}/checks", json={
        "check_type": "safety", "params": {"items": ["gloves"]}, "prompt": "戴手套",
    })
    assert r.status_code == 201, r.text
    cid = r.json()["id"]
    # list
    r = client.get(f"/api/v1/nodes/{nid}/checks")
    assert r.status_code == 200
    assert [c["id"] for c in r.json()] == [cid]
    # patch
    r = client.patch(f"/api/v1/checks/{cid}", json={"prompt": "请戴丁腈手套"})
    assert r.status_code == 200
    assert r.json()["prompt"] == "请戴丁腈手套"
    # delete
    r = client.delete(f"/api/v1/checks/{cid}")
    assert r.status_code == 204
    r = client.get(f"/api/v1/nodes/{nid}/checks")
    assert r.json() == []


def test_create_invalid_returns_422(client, step_node):
    r = client.post(f"/api/v1/nodes/{step_node.id}/checks", json={"check_type": "ocr", "params": {}})
    assert r.status_code == 422
```

> 注：若 `client` 夹具未自动注入认证身份，按本仓库既有 API 测试（如 `tests/test_teams_api.py`）的登录/头部方式补齐请求头——照抄该文件的认证夹具用法。

- [ ] **Step 2: Run to verify failure**

Run: `cd backend && pytest tests/test_node_checks_api.py -v`
Expected: FAIL（404，路由不存在）

- [ ] **Step 3: Write the router**

```python
# backend/app/routers/node_checks.py
"""核查点路由。Router 提交事务（与 nodes 路由一致）。"""
from __future__ import annotations

from fastapi import APIRouter, Depends, Response, status
from sqlalchemy.orm import Session

from app.billing.catalog import Feature
from app.deps import get_db, require_feature
from app.schemas.node_check import CheckCreateIn, CheckOut, CheckPatchIn
from app.services import node_check_service

router = APIRouter(
    tags=["node-checks"],
    dependencies=[Depends(require_feature(Feature.sop))],
)


@router.get("/api/v1/nodes/{node_id}/checks", response_model=list[CheckOut])
def list_checks(node_id: str, db: Session = Depends(get_db)) -> list[CheckOut]:
    return [CheckOut.model_validate(c) for c in node_check_service.list_checks(db, node_id)]


@router.post(
    "/api/v1/nodes/{node_id}/checks",
    response_model=CheckOut,
    status_code=status.HTTP_201_CREATED,
)
def create_check(node_id: str, payload: CheckCreateIn, db: Session = Depends(get_db)) -> CheckOut:
    c = node_check_service.create_check(db, node_id, payload.model_dump())
    db.commit()
    db.refresh(c)
    return CheckOut.model_validate(c)


@router.patch("/api/v1/checks/{check_id}", response_model=CheckOut)
def patch_check(check_id: str, payload: CheckPatchIn, db: Session = Depends(get_db)) -> CheckOut:
    c = node_check_service.patch_check(db, check_id, payload.model_dump(exclude_unset=True))
    db.commit()
    db.refresh(c)
    return CheckOut.model_validate(c)


@router.delete("/api/v1/checks/{check_id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_check(check_id: str, db: Session = Depends(get_db)) -> Response:
    node_check_service.delete_check(db, check_id)
    db.commit()
    return Response(status_code=status.HTTP_204_NO_CONTENT)
```

- [ ] **Step 4: Register the router**

在 `backend/app/main.py` 找到现有 `from app.routers import (...)` 与 `app.include_router(nodes.router)`（或类似），照葫芦：

```python
from app.routers import node_checks  # 加到 import 区
...
app.include_router(node_checks.router)  # 加到 nodes 注册旁边
```

Run（确认注册点）: `grep -n "include_router(nodes" backend/app/main.py`，把新行加在它后面。

- [ ] **Step 5: Run to verify pass**

Run: `cd backend && pytest tests/test_node_checks_api.py -v`
Expected: PASS（2 passed）

- [ ] **Step 6: 全量回归**

Run: `cd backend && pytest -q`
Expected: 既有测试全绿 + 新增用例通过。

- [ ] **Step 7: Commit**

```bash
git add backend/app/routers/node_checks.py backend/app/main.py backend/tests/test_node_checks_api.py
git commit -m "feat(checks): REST endpoints for node check points"
```

---

## Task 6: 前端 API 客户端

**Files:**
- Create: `frontend/src/api/checks.ts`

> 参照现有 `frontend/src/api/nodes.ts` 的 axios 实例与类型风格。

- [ ] **Step 1: 读现有 api 约定**

Run: `sed -n '1,40p' frontend/src/api/nodes.ts`
照其 import 的 http 客户端（如 `import { http } from './http'` 或 axios 实例）与导出风格。

- [ ] **Step 2: 写客户端**

```typescript
// frontend/src/api/checks.ts
import { http } from './http' // ← 若 nodes.ts 用别的实例名，改成一致

export interface NodeCheck {
  id: string
  node_id: string
  procedure_id: string
  check_type: 'ocr' | 'safety'
  modality: 'visual' | 'voice' | 'dual'
  severity: 'info' | 'warn' | 'critical'
  trigger: 'on_enter' | 'manual' | 'continuous'
  prompt: string
  keep_evidence: boolean
  confidence_threshold: number | null
  params: Record<string, unknown>
  sort_order: number
}

export type NodeCheckCreate = Omit<NodeCheck, 'id' | 'node_id' | 'procedure_id' | 'sort_order'> & {
  sort_order?: number
}

export const listChecks = (nodeId: string) =>
  http.get<NodeCheck[]>(`/api/v1/nodes/${nodeId}/checks`).then((r) => r.data)

export const createCheck = (nodeId: string, body: NodeCheckCreate) =>
  http.post<NodeCheck>(`/api/v1/nodes/${nodeId}/checks`, body).then((r) => r.data)

export const patchCheck = (checkId: string, body: Partial<NodeCheck>) =>
  http.patch<NodeCheck>(`/api/v1/checks/${checkId}`, body).then((r) => r.data)

export const deleteCheck = (checkId: string) =>
  http.delete(`/api/v1/checks/${checkId}`).then(() => undefined)
```

- [ ] **Step 3: 类型检查**

Run: `cd frontend && npm run type-check`（或 `npx vue-tsc --noEmit`）
Expected: 无新增类型错误。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/api/checks.ts
git commit -m "feat(checks): frontend api client for node checks"
```

---

## Task 7: 编写端「监护」面板

**Files:**
- Create: `frontend/src/components/editor/NodeCheckPanel.vue`
- Modify: `frontend/src/components/editor/NodeDetailPanel.vue`

> 目标：步骤详情面板里增一块「监护」，列出核查点、可加（类型动态表单）、可删。第一期类型只给 `ocr` / `safety`。

- [ ] **Step 1: 读步骤详情面板，找挂载点**

Run: `sed -n '1,120p' frontend/src/components/editor/NodeDetailPanel.vue`
确认：① 当前节点对象与 `kind` 字段怎么拿；② 用的 UI 库（Element Plus）组件风格。

- [ ] **Step 2: 写面板组件**

```vue
<!-- frontend/src/components/editor/NodeCheckPanel.vue -->
<script setup lang="ts">
import { ref, watch } from 'vue'
import { listChecks, createCheck, deleteCheck, type NodeCheck } from '@/api/checks'

const props = defineProps<{ nodeId: string }>()
const checks = ref<NodeCheck[]>([])
const loading = ref(false)
const adding = ref(false)
const draftType = ref<'ocr' | 'safety'>('ocr')
const ocrTarget = ref('')
const ocrMode = ref<'exact' | 'contains' | 'regex' | 'range'>('range')
const safetyItems = ref('gloves')
const prompt = ref('')

async function reload() {
  loading.value = true
  try { checks.value = await listChecks(props.nodeId) } finally { loading.value = false }
}

watch(() => props.nodeId, reload, { immediate: true })

async function add() {
  const params =
    draftType.value === 'ocr'
      ? { target_desc: ocrTarget.value, match_mode: ocrMode.value }
      : { items: safetyItems.value.split(',').map((s) => s.trim()).filter(Boolean) }
  await createCheck(props.nodeId, {
    check_type: draftType.value,
    modality: 'visual',
    severity: 'warn',
    trigger: 'on_enter',
    prompt: prompt.value,
    keep_evidence: true,
    confidence_threshold: null,
    params,
  })
  adding.value = false
  ocrTarget.value = ''; safetyItems.value = 'gloves'; prompt.value = ''
  await reload()
}

async function remove(id: string) {
  await deleteCheck(id)
  await reload()
}
</script>

<template>
  <div class="node-check-panel">
    <div class="hd">
      <span>监护核查点</span>
      <el-button size="small" @click="adding = !adding">+ 添加核查点</el-button>
    </div>

    <el-empty v-if="!loading && checks.length === 0 && !adding" description="暂无核查点" :image-size="48" />

    <ul class="check-list">
      <li v-for="c in checks" :key="c.id">
        <el-tag size="small">{{ c.check_type }}</el-tag>
        <span class="prompt">{{ c.prompt || '(无提示语)' }}</span>
        <el-button link type="danger" size="small" @click="remove(c.id)">删除</el-button>
      </li>
    </ul>

    <div v-if="adding" class="add-form">
      <el-select v-model="draftType" size="small" style="width: 120px">
        <el-option label="读数/OCR" value="ocr" />
        <el-option label="安全/合规" value="safety" />
      </el-select>

      <template v-if="draftType === 'ocr'">
        <el-input v-model="ocrTarget" size="small" placeholder="目标描述，如 压力表读数" />
        <el-select v-model="ocrMode" size="small" style="width: 120px">
          <el-option label="精确" value="exact" />
          <el-option label="包含" value="contains" />
          <el-option label="正则" value="regex" />
          <el-option label="数值范围" value="range" />
        </el-select>
      </template>
      <template v-else>
        <el-input v-model="safetyItems" size="small" placeholder="PPE 列表，逗号分隔，如 gloves,goggles" />
      </template>

      <el-input v-model="prompt" size="small" placeholder="操作者提示语" />
      <el-button type="primary" size="small" @click="add">保存</el-button>
    </div>
  </div>
</template>

<style scoped>
.node-check-panel { margin-top: 12px; }
.hd { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px; font-weight: 600; }
.check-list { list-style: none; padding: 0; margin: 0; }
.check-list li { display: flex; align-items: center; gap: 8px; padding: 4px 0; }
.check-list .prompt { flex: 1; color: #606266; font-size: 13px; }
.add-form { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 8px; }
</style>
```

> 若仓库不用 Element Plus，按 Step 1 看到的 UI 库替换 `el-*` 组件；逻辑不变。

- [ ] **Step 3: 集成进步骤详情面板**

在 `NodeDetailPanel.vue`：导入并在 `kind === 'step'` 的分支渲染：

```vue
<script setup lang="ts">
import NodeCheckPanel from './NodeCheckPanel.vue'
// ...既有代码
</script>

<template>
  <!-- ...既有步骤详情... -->
  <NodeCheckPanel v-if="node.kind === 'step'" :node-id="node.id" />
</template>
```
（`node` 取既有节点对象名——按 Step 1 实际变量名调整。）

- [ ] **Step 4: 类型检查 + 构建**

Run: `cd frontend && npm run type-check && npm run build`
Expected: 通过，无类型错误。

- [ ] **Step 5: 手动验证（跑起来看）**

用 `running-smartsop-dev` 技能启动（后端 8000 + 前端 5173），登录测试账号（`Devpass123!`），打开任一**草稿**程序编辑器：
1. 选中一个**步骤**节点 → 步骤详情里出现「监护核查点」；
2. 「+ 添加核查点」→ 选「安全/合规」→ items 填 `gloves` → 提示语「请戴手套」→ 保存 → 列表出现一条 `safety`；
3. 刷新页面，核查点仍在（已落库）；
4. 删除 → 列表清空。
Expected: 全部如上；后端 `tb_procedure_node_check` 有对应行。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/editor/NodeCheckPanel.vue frontend/src/components/editor/NodeDetailPanel.vue
git commit -m "feat(checks): authoring panel for step supervision check points"
```

---

## Self-Review（计划自检）

- **Spec 覆盖**：本计划实现 spec §6 编写侧「步骤挂 N 个核查点 / 公共字段 / 类型专属字段」的第一期子集（ocr+safety），D2/D3。模态 `dual`（语音双核）字段已建（modality 枚举含 voice/dual），运行时消费留后端执行服务计划。✅
- **占位符扫描**：无 TBD/TODO；每个代码步骤给了完整代码；唯一"按实际调整"处（main.py 注册点、前端 http 实例名、UI 库、`client` 认证夹具）均给了**定位命令**与**参照文件**，非空泛占位。✅
- **类型一致**：`ProcedureNodeCheck` 字段 ↔ `CheckOut/CheckCreateIn` ↔ service `create_check`/`patch_check` 的 key ↔ 前端 `NodeCheck` 接口，命名逐一对齐（check_type/modality/severity/trigger/prompt/keep_evidence/confidence_threshold/params/sort_order）。service 方法名 `list_checks/create_check/patch_check/delete_check` 在 Task 4/5 一致。✅
- **范围**：单一竖切（监护期望编写），可独立 pytest + 手动验证，符合"每份计划产出可工作可测软件"。SOP 参考关系/图纸附件为 Web 端第二份计划。

## 依赖与后续

- **本计划无前置**（纯扩展现有仓库）。
- 后续 Web 端计划：①SOP 参考关系（编写/执行参考）+ 图纸/附件（D22）；②执行记录查看页（待后端执行服务计划产出数据）。
- 之后才是后端执行服务（SP1）与移动端（SP3，最后）。

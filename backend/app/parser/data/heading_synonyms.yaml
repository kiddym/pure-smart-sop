# 标题样式中文同义词词典（内置默认层，§27.4 / Q208 / Q344）
#
# 用途：styles.xml 反查时，自定义命名样式（非标准 heading）的 name 命中本表 →
# 映射到对应标题层级。随代码发布；运行时组织级 heading_style_map（DB 层，M4 前端
# 「记住此样式」写入）以注入参数 style_overrides 覆盖本表（延后，见 Q344）。
#
# 命中记 confidence=1.0（样式类信号），不进 review。
# 实测：TP试验程序.docx 的 65 个标题全靠「章节标题」命中（word-parser-solution §5.4）。

level_1:
  - 章节标题
  - 章标题
  - 一级标题
  - 标题一
level_2:
  - 节标题
  - 小节标题
  - 二级标题
  - 标题二
level_3:
  - 条标题
  - 三级标题
  - 标题三
